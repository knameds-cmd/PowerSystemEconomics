# Config

This folder stores shareable configuration templates.

- `data_sources.template.toml`: source metadata, endpoint references, and expected parameters

Put real secrets in a local `.env` file instead of committing them here.
