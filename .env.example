# Copy this file to `.env` for local-only use.
# Never commit the real key to GitHub.

DATA_GO_KR_SERVICE_KEY=replace_with_decoded_service_key
ODCLOUD_SERVICE_KEY=replace_with_decoded_service_key_if_needed
